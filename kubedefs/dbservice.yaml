apiVersion: v1
kind: Service
metadata:
  name: vprodb
spec:
  selector:
    app: vprodb
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: vprodb-port
    # service avilable just inside the cluster 
    protocol: TCP
